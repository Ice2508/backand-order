{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "singularName": "order",
    "pluralName": "orders",
    "displayName": "order"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "productId": {
      "type": "integer",
      "required": true,
      "unique": false
    },
    "amount": {
      "type": "integer"
    },
    "statusOrder": {
      "type": "enumeration",
      "default": "pending",
      "enum": [
        "pending",
        "paid",
        "canceled"
      ]
    },
    "paymentId": {
      "type": "string"
    }
  }
}
